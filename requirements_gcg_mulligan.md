# 要件定義書
## ガンダムカードゲーム マリガン期待値計算アプリ

**文書バージョン:** 1.0.0  
**作成日:** 2026年05月22日  
**ステータス:** ドラフト

---

## 目次

1. プロジェクト概要
2. ゲームルール参照（基礎知識）
3. 機能要件
4. 非機能要件
5. データモデル定義
6. 画面設計
7. 計算ロジック仕様
8. テスト要件
9. 技術スタック
10. 制約事項・前提条件

---

## 1. プロジェクト概要

### 1-1. 目的

ガンダムカードゲーム（以下 GCG）プレイヤーが、デッキを仮想的に構築し、ゲーム開始時の初期手札とマリガン後の手札における「コスト期待値」を定量的に把握できるWebアプリケーションを提供する。

これにより、プレイヤーは以下を実現できる。

- デッキのコスト分布が初期手札に与える影響を数値で確認できる
- マリガンを行うことで手札のコスト改善が期待できるかを事前に評価できる
- キーカードが初期手札に入る確率を把握し、デッキ構築を最適化できる

### 1-2. スコープ

**対象範囲（In-Scope）**

- デッキ構築機能（最大50枚）
- 初期手札コスト期待値の数学的計算
- マリガン後の手札コスト期待値の計算
- ランダムな初期手札のシミュレーション（テストドロー）
- キーカード設定と初期手札への含有率表示

**対象外（Out-of-Scope）**

- オンライン対戦機能
- 実際のカードデータベースとの連携（カード情報は手動入力）
- デッキコードの保存・共有（将来拡張）

---

## 2. ゲームルール参照（基礎知識）

アプリの計算ロジックは、GCG 総合ルール Ver.1.5.0（最終更新：2026年01月30日）および公式サイトの遊び方ガイドに準拠する。

### 2-1. デッキ構成ルール

| 項目 | ルール |
|------|--------|
| デッキ枚数 | 50枚ちょうど |
| 同一カードの枚数上限 | 同一カードNo.のカードは4枚まで |
| 使用可能色 | 2色まで |
| デッキ構成カードタイプ | ユニット・パイロット・コマンド・ベース |
| リソースデッキ | 10枚（別途構築。本アプリの計算対象外） |

### 2-2. ゲーム準備とマリガンルール

| ステップ | 内容 |
|----------|------|
| 初期手札 | デッキから5枚ドロー |
| マリガン権 | 先攻から順に1回ずつ、任意で実施可能 |
| マリガン方法 | 手札5枚全部をデッキ下部に戻し、新たにデッキから5枚引く |
| シャッフル | マリガン後にデッキをシャッフルする |
| マリガン回数 | 1回限り（再マリガン不可） |

> **参照：** GCG公式サイト「ゲームの遊び方」、TCGカタログ（マリガン仕様）

### 2-3. カード情報（計算に使用する属性）

| 属性 | 説明 | 備考 |
|------|------|------|
| カードNo. | カード識別子 | 同一No.は4枚上限 |
| カード名 | 固有名称 | |
| カードタイプ | ユニット/パイロット/コマンド/ベース | |
| 色 | 青/緑/赤/紫/白 | 最大2色 |
| Lv.（レベル） | プレイに必要なリソース枚数 | |
| コスト | プレイ時に消費するアクティブリソース数 | 期待値計算の主対象 |

---

## 3. 機能要件

### 3-1. カード管理機能

#### FR-01: カード追加

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-01 |
| 機能名 | カードをデッキに追加する |
| 入力 | カードNo.、カード名、カードタイプ、色、Lv.、コスト、枚数（1〜4）、キーカードフラグ |
| 処理 | 同一カードNo.の合計枚数が4枚を超えないよう検証。デッキ合計が50枚を超えないよう検証 |
| 出力 | デッキリストへの追加。デッキ合計枚数の更新 |
| バリデーション | コストは0以上の整数。Lv.は0以上の整数。カード名は必須（空文字不可）。枚数は1〜4の整数 |

#### FR-02: カード編集・削除

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-02 |
| 機能名 | デッキ内カードを編集・削除する |
| 処理（編集） | 枚数変更時は4枚上限・デッキ50枚上限を再検証 |
| 処理（削除） | カードをデッキから除外し、枚数カウントを更新 |

#### FR-03: キーカード設定

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-03 |
| 機能名 | カードにキーカードフラグを設定する |
| 説明 | 特定のカードを「キーカード」として設定できる。キーカードが初期手札（またはマリガン後手札）に1枚以上含まれる確率を計算・表示する |
| 複数設定 | 複数カードにキーカードフラグをつけられる。その場合「いずれか1枚以上が初期手札に含まれる確率」を計算する |

#### FR-04: デッキ保存・読込

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-04 |
| 機能名 | デッキをブラウザのローカルストレージに保存・読込する |
| 保存項目 | デッキ名、カードリスト（全属性）、キーカード設定 |
| 上限 | 最大5デッキまで保存可能 |

---

### 3-2. 期待値計算機能

#### FR-05: 初期手札コスト期待値計算（マリガンなし）

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-05 |
| 機能名 | マリガンなし初期手札のコスト期待値を計算する |
| 前提 | デッキが50枚構築済みであること |
| 計算方法 | 超幾何分布に基づき、デッキ50枚から5枚を無作為に引いたときのコスト合計の期待値を求める。各カードiについて期待値への寄与は `コスト_i × (枚数_i / 50) × 5` |
| 出力値 | 手札5枚の総コスト期待値（小数点第2位まで）、平均コスト/1枚（小数点第2位まで） |

**期待値計算式（詳細）:**

手札の総コスト期待値 `E[TotalCost]` は以下で求める。

```
E[TotalCost] = Σ( cost_i × n_i / 50 × 5 )
             = 5 × Σ( cost_i × n_i ) / 50
```

ただし `n_i` はカードiのデッキ内枚数、`cost_i` はカードiのコスト。

#### FR-06: マリガン後手札コスト期待値計算

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-06 |
| 機能名 | マリガン後の最終手札コスト期待値を計算する |
| 前提 | GCGのマリガンは「全5枚をデッキ下に戻し→シャッフルして5枚新たに引く」ため、2度目も同じデッキ構成から5枚引くと同等に扱える |
| 計算方法 | 以下の3ケースを加重合算する。(1)マリガンしない場合のコスト期待値、(2)マリガンした場合のコスト期待値（FR-05と同値。デッキ構成は変化しないため）。実際の期待値計算では「マリガンを行う場合」と「行わない場合」の比較を提示する形とする |
| 出力値 | マリガンなし期待値、マリガンあり期待値（参考：両者は同値になることをUIで説明）、コスト分散、標準偏差 |

> **補足:** GCGのマリガンはデッキをシャッフルしてから引き直すため、マリガン後の期待値はデッキ構成が変わらない限りマリガン前と同値になる。このアプリではその旨を明示しつつ、「コスト分布のばらつき（分散・標準偏差）」を提示することで、マリガンの有用性を確率論的に示す。

#### FR-07: コスト分布表示

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-07 |
| 機能名 | 手札5枚の総コスト分布をグラフで表示する |
| 計算方法 | 超幾何分布の畳み込みにより、手札総コストが各値となる確率を計算する |
| 出力 | 棒グラフ（横軸: 総コスト値、縦軸: 確率）。マリガンなし・マリガンあり（2回試行）の分布を重ねて表示 |

#### FR-08: キーカード含有確率計算

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-08 |
| 機能名 | キーカードが初期手札に含まれる確率を計算する |
| 計算方法 | 超幾何分布を使用。キーカードのデッキ内合計枚数をK、デッキ枚数を50、手札枚数を5として「1枚以上引く確率」を計算する |

**キーカード含有確率計算式:**

```
P(1枚以上) = 1 - P(0枚) = 1 - C(50-K, 5) / C(50, 5)
```

マリガンあり（2回試行、独立）:

```
P_マリガン(1枚以上) = 1 - P(0枚)^2
                    = 1 - (C(50-K,5)/C(50,5))^2
```

---

### 3-3. シミュレーション機能（テストドロー）

#### FR-09: ランダム初期手札シミュレーション

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-09 |
| 機能名 | ランダムな初期手札を1セット引いて表示する |
| 処理 | フィッシャー–イェーツ シャッフルアルゴリズムでデッキをシャッフルし、上から5枚を初期手札として表示 |
| 表示内容 | 手札5枚の一覧（カード名、タイプ、コスト、キーカードマーク）、手札の総コスト合計、キーカードの有無 |

#### FR-10: マリガン後のシミュレーション

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-10 |
| 機能名 | マリガンを実施して新たな手札を引き直す |
| 処理 | 現在の手札を全てデッキに戻し→シャッフル→上から5枚引く |
| 表示内容 | マリガン後手札5枚の一覧、手札総コスト、マリガン前後のコスト比較 |
| 制約 | 1回のシミュレーションでのマリガンは1回のみ |

#### FR-11: シミュレーションの統計集計

| 項目 | 仕様 |
|------|------|
| 機能ID | FR-11 |
| 機能名 | 複数回シミュレーション結果の統計を表示する |
| 処理 | 100回 / 1000回 / 10000回の試行をまとめて実行し、結果を集計する |
| 出力 | 平均コスト、標準偏差、キーカード含有率（実測）、コスト分布ヒストグラム |

---

## 4. 非機能要件

### 4-1. パフォーマンス

| 要件ID | 内容 |
|--------|------|
| NFR-01 | 期待値計算は1秒以内に完了すること |
| NFR-02 | 10000回のシミュレーションは3秒以内に完了すること |
| NFR-03 | 初回ページロードは3秒以内（LCP基準） |

### 4-2. 可用性・互換性

| 要件ID | 内容 |
|--------|------|
| NFR-04 | 対象ブラウザ: Chrome 最新版、Firefox 最新版、Safari 最新版、Edge 最新版 |
| NFR-05 | レスポンシブデザイン対応。PC（1280px以上）およびスマートフォン（375px以上）で正常動作 |
| NFR-06 | インターネット接続なしでも動作すること（オフライン対応） |

### 4-3. 型安全性・品質

| 要件ID | 内容 |
|--------|------|
| NFR-07 | TypeScript（strict mode）を使用し、型チェックによる安全性を確保する |
| NFR-08 | テストカバレッジ90%以上を維持する |
| NFR-09 | ESLint + Prettierによるコード品質管理を実施する |

---

## 5. データモデル定義

### 5-1. カード型

```typescript
type CardType = 'ユニット' | 'パイロット' | 'コマンド' | 'ベース';
type CardColor = '青' | '緑' | '赤' | '紫' | '白';

interface Card {
  id: string;           // UUID（アプリ内管理用）
  cardNo: string;       // カードNo.（同一No.は4枚上限の判定に使用）
  name: string;         // カード名
  cardType: CardType;   // カードタイプ
  color: CardColor;     // 色
  level: number;        // Lv.（0以上の整数）
  cost: number;         // コスト（0以上の整数）
  isKeyCard: boolean;   // キーカードフラグ
}
```

### 5-2. デッキエントリ型

```typescript
interface DeckEntry {
  card: Card;
  count: number;        // デッキ内枚数（1〜4）
}
```

### 5-3. デッキ型

```typescript
interface Deck {
  id: string;           // UUID
  name: string;         // デッキ名
  entries: DeckEntry[]; // カード一覧
  createdAt: string;    // 作成日時（ISO8601）
  updatedAt: string;    // 更新日時（ISO8601）
}
```

バリデーション制約:

- `entries` の全 `count` 合計 = 50
- 同一 `cardNo` の `count` 合計 ≤ 4
- デッキで使用されている色の種類 ≤ 2

### 5-4. 計算結果型

```typescript
interface ExpectedValueResult {
  averageCostPerCard: number;      // 1枚あたりの平均コスト
  totalHandCost: number;           // 手札5枚の総コスト期待値
  variance: number;                // 分散
  standardDeviation: number;       // 標準偏差
  costDistribution: Record<number, number>; // {コスト値: 確率} のマップ
}

interface KeyCardProbability {
  cardIds: string[];               // キーカードのID一覧
  totalKeyCardCount: number;       // デッキ内キーカード合計枚数
  probInitialHand: number;         // 初期手札に1枚以上含まれる確率（0〜1）
  probAfterMulligan: number;       // マリガン後に1枚以上含まれる確率（0〜1）
}

interface SimulationResult {
  hand: DeckEntry[];               // ドローした手札
  totalCost: number;               // 手札の総コスト
  containsKeyCard: boolean;        // キーカードを含むか
  mulliganCount: number;           // マリガン回数（0 or 1）
}
```

---

## 6. 画面設計

### 6-1. 画面一覧

| 画面ID | 画面名 | 説明 |
|--------|--------|------|
| SC-01 | デッキ編集画面 | カードの追加・編集・削除、キーカード設定 |
| SC-02 | 期待値ダッシュボード | コスト期待値・分布・キーカード確率の一覧表示 |
| SC-03 | テストドロー画面 | ランダムドローとマリガンシミュレーション |
| SC-04 | 統計結果画面 | 複数回シミュレーション結果の集計表示 |

### 6-2. SC-01: デッキ編集画面

```
┌─────────────────────────────────────────────────────────┐
│  🃏 GCG マリガン期待値計算アプリ          [デッキ保存]  │
├──────────────────────────┬──────────────────────────────┤
│  デッキ名: [          ] [デッキ選択 ▼]  │                              │
│                            │  カード追加フォーム          │
│  ─── デッキ一覧 ──────    │  ─────────────────────────  │
│  合計: 38 / 50枚           │  カードNo.:  [        ]     │
│  使用色: 青, 赤            │  カード名:  [        ]     │
│                            │  タイプ:    [ユニット ▼]   │
│  [ユニット] コスト: 3      │  色:        [青 ▼]         │
│  ガンダム          ×3  🔑 │  Lv.:       [  3  ]         │
│  [ユニット] コスト: 2      │  コスト:    [  3  ]         │
│  量産型ザク        ×4     │  枚数:      [  1  ]         │
│  [パイロット] コスト: 1    │  キーカード: [✓]            │
│  アムロ・レイ      ×2  🔑 │                              │
│  ...                       │  [カードを追加する]          │
│  [+ カードを追加]          │                              │
└──────────────────────────┴──────────────────────────────┘
```

### 6-3. SC-02: 期待値ダッシュボード

```
┌─────────────────────────────────────────────────────────┐
│  📊 期待値ダッシュボード          デッキ: 「青赤デッキ」 │
├─────────────────────────────────────────────────────────┤
│  ■ 初期手札（5枚）コスト期待値                          │
│                                                          │
│  平均コスト合計:  10.40    1枚あたり平均コスト:  2.08   │
│  分散:  2.35               標準偏差:  1.53               │
│                                                          │
│  ■ マリガン後（参考）                                   │
│  マリガン後の期待値は同値（10.40）です。                 │
│  ただしマリガンにより「低コスト手札引き直し」の           │
│  チャンスを1回得られます。コスト分布を参照ください。     │
│                                                          │
├─────────────────────────────────────────────────────────┤
│  ■ 手札総コスト分布（確率グラフ）                       │
│  [棒グラフ: 横軸=総コスト 0〜20+, 縦軸=確率%]           │
│                                                          │
├─────────────────────────────────────────────────────────┤
│  ■ キーカード含有確率                                   │
│  キーカード: ガンダム(×3), アムロ・レイ(×2)  合計5枚   │
│                                                          │
│  初期手札に1枚以上含まれる確率:  40.66 %                 │
│  マリガン後（2回試行）に含まれる確率:  64.96 %           │
└─────────────────────────────────────────────────────────┘
```

### 6-4. SC-03: テストドロー画面

```
┌─────────────────────────────────────────────────────────┐
│  🎴 テストドロー                    [新たにドロー]       │
├─────────────────────────────────────────────────────────┤
│  初期手札（5枚）                    総コスト: 12         │
│                                                          │
│  1. [ユニット] 🔑 ガンダム          コスト: 3           │
│  2. [ユニット]    ガンキャノン       コスト: 4           │
│  3. [パイロット]  量産型パイロット   コスト: 1           │
│  4. [コマンド]    強化作戦           コスト: 2           │
│  5. [ユニット]    ザクII             コスト: 2           │
│                                                          │
│  ✅ キーカードを引きました！                             │
│                                                          │
│  [マリガンを実施する]                                    │
├─────────────────────────────────────────────────────────┤
│  ■ 統計シミュレーション                                  │
│  [100回実行]  [1000回実行]  [10000回実行]                │
└─────────────────────────────────────────────────────────┘
```

---

## 7. 計算ロジック仕様

### 7-1. 超幾何分布による期待値計算

GCGの初期手札ドローは非復元抽出（デッキからカードを引き、引いたカードは戻さない）であるため、**超幾何分布**を用いて計算する。

**超幾何分布の確率質量関数:**

```
P(X = k) = C(K, k) × C(N-K, n-k) / C(N, n)
```

- `N`: 母集団サイズ（デッキ枚数 = 50）
- `K`: 注目カードのデッキ内枚数
- `n`: ドロー枚数（手札 = 5）
- `k`: 注目カードを引く枚数

**手札総コストの期待値（線形性を利用）:**

各カードiの手札コスト期待値は独立でないが、期待値の線形性により以下が成立する。

```
E[手札総コスト] = Σ_i ( cost_i × E[手札内のカードiの枚数] )
               = Σ_i ( cost_i × n_i × 5 / 50 )
               = (5/50) × Σ_i ( cost_i × n_i )
```

**分散の計算（超幾何分布の分散の合計）:**

```
Var[カードiの手札内枚数] = n × K_i/N × (N-K_i)/N × (N-n)/(N-1)
                        = 5 × n_i/50 × (50-n_i)/50 × 45/49

Var[手札総コスト] = Σ_i ( cost_i^2 × Var[カードiの手札内枚数] )
                  + 2 × Σ_{i<j} ( cost_i × cost_j × Cov[i,j] )

Cov[i,j] = -n × K_i/N × K_j/N × (N-n)/(N-1)   (i ≠ j)
```

> **注:** 計算の簡略化のため、分散計算においても各カードを独立とみなして近似することも許容するが、正確な値を用いることが望ましい。

### 7-2. キーカード含有確率計算

デッキ内のキーカード合計枚数を `K`、デッキ枚数50、手札枚数5とする。

```
P(0枚) = C(50-K, 5) / C(50, 5)
P(1枚以上) = 1 - P(0枚)
```

マリガンあり（マリガンは全手札引き直し、デッキ構成変化なし）:

```
P_マリガン(0枚) = P(0枚) × P(0枚) = P(0枚)^2
P_マリガン(1枚以上) = 1 - P(0枚)^2
```

### 7-3. シャッフルアルゴリズム

Fisher–Yates シャッフルを使用する。

```typescript
function fisherYatesShuffle<T>(array: T[]): T[] {
  const result = [...array];
  for (let i = result.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [result[i], result[j]] = [result[j], result[i]];
  }
  return result;
}
```

---

## 8. テスト要件

### 8-1. テスト方針

| 項目 | 方針 |
|------|------|
| テストフレームワーク | Vitest（Vite環境との親和性が高く高速） |
| カバレッジツール | @vitest/coverage-v8 |
| カバレッジ目標 | ステートメント・ブランチ・関数・行 すべて90%以上 |
| テスト種別 | ユニットテスト、統合テスト |

### 8-2. テスト対象と必須テストケース

#### 計算ロジック（最重要）

| テストID | 対象関数 | テスト内容 |
|----------|----------|------------|
| T-CALC-01 | `calculateExpectedCost` | 50枚均一コストデッキで期待値が正しいか |
| T-CALC-02 | `calculateExpectedCost` | コスト0のカードのみのデッキで期待値=0 |
| T-CALC-03 | `calculateExpectedCost` | デッキが50枚未満のとき例外をスロー |
| T-CALC-04 | `calculateVariance` | 既知の分散値との比較 |
| T-CALC-05 | `calculateKeyCardProbability` | K=4,N=50,n=5 のとき確率値が正しいか |
| T-CALC-06 | `calculateKeyCardProbability` | K=0 のとき確率=0 |
| T-CALC-07 | `calculateKeyCardProbability` | K=50 のとき確率=1 |
| T-CALC-08 | `calculateKeyCardProbabilityWithMulligan` | 2回試行の確率が正しいか |
| T-CALC-09 | `combination` | C(50,5)=2118760 であること |
| T-CALC-10 | `combination` | C(n,0)=1 であること |
| T-CALC-11 | `combination` | C(n,n)=1 であること |
| T-CALC-12 | `combination` | k>n のとき 0 を返す |

#### デッキバリデーション

| テストID | 対象関数 | テスト内容 |
|----------|----------|------------|
| T-VAL-01 | `validateDeck` | 合計50枚で合格 |
| T-VAL-02 | `validateDeck` | 合計49枚でエラー |
| T-VAL-03 | `validateDeck` | 合計51枚でエラー |
| T-VAL-04 | `validateDeck` | 同一カードNo.5枚でエラー |
| T-VAL-05 | `validateDeck` | 同一カードNo.4枚で合格 |
| T-VAL-06 | `validateDeck` | 3色使用でエラー |
| T-VAL-07 | `validateDeck` | 2色使用で合格 |
| T-VAL-08 | `validateCard` | コストに負数でエラー |
| T-VAL-09 | `validateCard` | カード名が空文字でエラー |
| T-VAL-10 | `validateCard` | Lv.に小数でエラー |

#### シャッフル・シミュレーション

| テストID | 対象関数 | テスト内容 |
|----------|----------|------------|
| T-SIM-01 | `fisherYatesShuffle` | シャッフル後も要素数が変わらない |
| T-SIM-02 | `fisherYatesShuffle` | シャッフル後も全要素が含まれる |
| T-SIM-03 | `drawHand` | 5枚引けること |
| T-SIM-04 | `drawHand` | デッキが5枚未満のとき例外をスロー |
| T-SIM-05 | `simulateMulligan` | マリガン後も5枚引けること |
| T-SIM-06 | `runMultipleSimulations` | 指定回数のシミュレーションが実行されること |
| T-SIM-07 | `runMultipleSimulations` | 実測キーカード含有率が確率論的に妥当な範囲に収まる（1000回試行） |

#### データ永続化

| テストID | 対象関数 | テスト内容 |
|----------|----------|------------|
| T-STORE-01 | `saveDeck` | デッキが正常に保存される |
| T-STORE-02 | `loadDeck` | 保存したデッキが正確に復元される |
| T-STORE-03 | `saveDeck` | 6デッキ目の保存でエラーをスロー |
| T-STORE-04 | `deleteDeck` | 存在するデッキが削除される |
| T-STORE-05 | `deleteDeck` | 存在しないIDで削除しようとしてもエラーにならない |

### 8-3. カバレッジ設定例（vitest.config.ts）

```typescript
export default defineConfig({
  test: {
    coverage: {
      provider: 'v8',
      thresholds: {
        statements: 90,
        branches: 90,
        functions: 90,
        lines: 90,
      },
      include: ['src/**/*.ts'],
      exclude: ['src/**/*.test.ts', 'src/main.ts'],
    },
  },
});
```

---

## 9. 技術スタック

### 9-1. フロントエンド

| 技術 | バージョン | 用途 |
|------|-----------|------|
| Node.js | 20.x LTS | 実行環境 |
| TypeScript | 5.x | 型安全な開発 |
| React | 18.x | UIフレームワーク |
| Vite | 5.x | ビルドツール・開発サーバー |
| Chart.js or Recharts | 最新安定版 | コスト分布グラフ描画 |

### 9-2. テスト・品質管理

| 技術 | バージョン | 用途 |
|------|-----------|------|
| Vitest | 1.x | テストフレームワーク |
| @vitest/coverage-v8 | 1.x | カバレッジ計測 |
| @testing-library/react | 14.x | Reactコンポーネントテスト |
| ESLint | 8.x | 静的解析 |
| Prettier | 3.x | コードフォーマット |

### 9-3. ディレクトリ構成（推奨）

```
gcg-mulligan-app/
├── src/
│   ├── components/       # Reactコンポーネント
│   │   ├── DeckEditor/
│   │   ├── Dashboard/
│   │   ├── TestDraw/
│   │   └── Statistics/
│   ├── logic/            # 計算ロジック（フレームワーク非依存）
│   │   ├── calculator.ts         # 期待値・確率計算
│   │   ├── simulator.ts          # シミュレーション
│   │   └── validator.ts          # バリデーション
│   ├── store/            # 状態管理・永続化
│   │   └── deckStore.ts
│   ├── types/            # 型定義
│   │   └── index.ts
│   └── main.tsx
├── tests/
│   ├── logic/
│   │   ├── calculator.test.ts
│   │   ├── simulator.test.ts
│   │   └── validator.test.ts
│   └── store/
│       └── deckStore.test.ts
├── vitest.config.ts
├── tsconfig.json
└── package.json
```

---

## 10. 制約事項・前提条件

### 10-1. ルール上の制約

- マリガンは先攻から順に各1回。本アプリでは「1プレイヤー視点」の計算を提供し、先後の差異は考慮しない
- GCGのマリガンは全5枚を戻してシャッフル後に引き直すため、2度目のドローもデッキ構成は変化しない（期待値は同値）。この点をUIで明示する
- リソースデッキはゲーム準備で使用されるが、初期手札のドロー対象はメインデッキのみ。本アプリはメインデッキのみを計算対象とする
- シールド配置（6枚）はデッキから行われるが、本アプリでは「シールド配置前」のデッキから手札を引くものとして計算を簡略化する

> **注:** 正確には手札ドローはシールド配置後に行われる。完全な精度を求める場合は「50枚デッキからシールド用6枚を除いた44枚から5枚引く」として計算する拡張版の実装も検討すること。

### 10-2. 技術的制約

- カードデータは外部DBとの連携なし。ユーザーが手動でカード情報を入力する
- ブラウザのローカルストレージを使用するため、デバイスをまたいだデータ同期は行わない

### 10-3. 将来拡張候補

- URLクエリパラメータによるデッキ共有
- GCGカードデータAPIとの連携（公式APIが提供される場合）
- シールド配置を考慮した精密計算モード（44枚から5枚引くモデル）
- マリガン戦略シミュレーター（「キーカードなしならマリガン」戦略の期待値計算）

---

*本文書は GCG 総合ルール Ver.1.5.0（2026年01月30日更新）および GCG 公式サイトのゲームガイドに基づき作成。*
